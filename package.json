{
  "name": "coderpuzzle-problems-local-tooling",
  "private": true,
  "description": "Pins the local node toolchain to the versions the runner image builds with (runner/Dockerfile ARGs). The image is authoritative; this file exists so a local npm install cannot silently drift the format gate. node_modules/ is gitignored.",
  "dependencies": {
    "prettier": "3.9.6",
    "prettier-plugin-java": "2.10.3",
    "sql-formatter": "15.8.2"
  }
}
